# Plan: Remove Legacy Network-Driven Taste Loss and Network Turnover Content

The user has requested to:
1. Thoroughly review the entire manuscript to remove any vestigial content about network-driven taste loss and discrete time event history models (which are no longer being pursued).
2. Remove the section describing "network turnover indicators" (since these models are no longer being estimated).
3. Move/integrate the recent network turnover literature (e.g., Hollstein 2023, Marsden 2024, etc.) from the deleted section into the Introduction instead.
4. Ensure the manuscript structure and logical flow are maintained and up-to-date with the latest purely cultural-capital/Mundlak analysis.

## Steps

### Step 1: Read the Manuscript and Identify Sections to Remove
I have already reviewed `manuscript_research_note.tex`. The main sections to remove/modify are:
- `\subsubsection{Network turnover indicators}` (lines 106-121) and the list of 7 indicators (changes in friends, orgs, educ, work, geography, marriage, children).
- `\subsection{Network driven taste loss}` (around line 135).
- `\subsubsection{Analytic Strategy}` describing the discrete-time event history analysis.
- `\subsubsection{Results}` detailing the frequency of taste loss, the `pcs_taste_change_modern.tex` table, and the `taste_retention.png` plot.

*Wait*, the user says "the paper still talks about cultural taste loss discrete time event histories that we are no longer pursuing. Also we don't need the section on network tunover indicators". 
If we remove the discrete time event history completely, we are removing the finding about "taste loss is a remarkably rare event" (98% retention). I should double check if the user meant to remove the *entire* section or just the event history regression *models* (which used the network turnover indicators).
The prompt explicitly says: "the paper still talks about cultural taste loss discrete time event histories that we are no longer pursuing." This implies removing the regression models. Does it mean removing the descriptive finding that taste loss is rare? Let's assume we remove the event history modeling entirely, but we might want to keep the descriptive retention plot if it's not part of the "event history model". Wait, the prompt says "the paper still talks about cultural taste loss discrete time event histories that we are no longer pursuing" - so we definitely remove the event history section. 

### Step 2: Remove the Sections
- Delete `\subsubsection{Network turnover indicators}` and its contents.
- Delete `\subsection{Network driven taste loss}` and its sub-sections (Analytic Strategy, Results).
- Delete the `\input{Tabs/pcs_taste_change_modern.tex}`.
- Remove the `\includegraphics{Plots/taste_retention.png}` if it's bound to that section. (Actually, the descriptive retention is part of that section, maybe we keep a brief sentence about high stability without the event history modeling, or remove it entirely. The abstract mentions "descriptive event-history tracking and Mundlak Poisson models". If we remove event-history, we should update the abstract too).

### Step 3: Update the Introduction
- Ensure the citations `\citep{hollstein2023, marsden2024, vacchiano2024, lin2022, weiss2022, burt2000, suitor1997}` are properly integrated into the introduction paragraph about network turnover. (I actually already did this partially in a previous edit, but I will make sure it reads well and encompasses the life-course transitions previously described).

### Step 4: Update the Abstract and Introduction
- Remove mentions of "descriptive event-history tracking" from the Abstract.
- Remove RQ1 and RQ2 from the Introduction if they specifically reference the taste loss models (e.g., "(1) Do cultural tastes exhibit the high volatility implied by network transmission models... (2) When cultural taste loss does occur, is it primarily driven by life-course events..."). Wait, I already removed the old RQ2 in a previous edit, so it's currently: "(1) Do cultural tastes exhibit the high volatility implied by network transmission models, or the high stability expected by cultural capital models?" We need to re-align the research questions with the remaining Mundlak models.

### Step 5: Update the Discussion
- Remove the paragraph in the discussion that states: "The network-theoretic assumption that relational disruption drives mass taste loss is decisively rejected by the simple fact that taste abandonment is an extreme rarity across all measured domains." (Or adjust it if we are keeping the descriptive plot but just dropping the regression).
Given the prompt: "there are no vestigial or legacy content or discussion of analyses that we are no longer doing" -> We will purge the event history / taste loss regression completely.

### Final Verification
- Run `pdflatex` to ensure it compiles.
- Delete `Tabs/pcs_taste_change_modern.tex` and `Plots/taste_retention.png` and their generating code if the user confirms. (Since I'm in Plan mode, I will just outline this).

I will present this plan to the user.
